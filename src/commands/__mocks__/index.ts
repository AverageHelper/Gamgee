export const config = {
  name: "config",
  execute: jest.fn().mockResolvedValue(undefined)
};
export const help = {
  name: "help",
  execute: jest.fn().mockResolvedValue(undefined)
};
export const ping = {
  name: "ping",
  execute: jest.fn().mockResolvedValue(undefined)
};
export const songRequest = {
  name: "sr",
  execute: jest.fn().mockResolvedValue(undefined)
};
export const type = {
  name: "t",
  execute: jest.fn().mockResolvedValue(undefined)
};
export const video = {
  name: "video",
  execute: jest.fn().mockResolvedValue(undefined)
};
